<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Colectif" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorPrimary">@color/menu_seleccionado_claro</item>
        <item name="android:statusBarColor">@color/texto</item>

    </style>

    <style name="Theme.Colectif" parent="Base.Theme.Colectif" />
</resources>